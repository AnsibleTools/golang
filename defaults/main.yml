---
# defaults file for go
playbook_name: "Golang"

golang_version: "1.19.1"
golang_url: "https://golang.org/dl"
golang_archive: "go{{ golang_version }}.linux-amd64.tar.gz"
#golang_install_dir: "/opt/golang/go-{{ golang_version }}"

golang_install_dir: "/usr/local/go"
